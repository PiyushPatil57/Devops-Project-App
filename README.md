# Sample Node.js App

A simple REST API for DevOps CI/CD practice.

## Endpoints
- `/` → Welcome message
- `/status` → Health check
- `/api/greet/:name` → Returns greeting message

## Run Locally
1. Install dependencies:
2. Start server:
3. Test endpoints:

# Sample DevOps Node.js Application

This repository contains a basic Node.js + Express application built to learn and implement DevOps tools and practices.

### 🔧 DevOps Tools Used
- Docker: Containerization
- Jenkins: CI/CD automation
- Docker Hub: Image registry
- Ansible: Configuration management & deployment
- SonarQube: Code quality scanning
- Prometheus & Grafana: Monitoring and visual dashboards

### 🔍 Endpoints
- `GET /` → Welcome message
- `GET /status` → Health check
- `GET /api/greet/:name` → Sample API

---

## 🚀 Goal
Deploy this app automatically to a server using Jenkins Pipeline and Ansible after every GitHub commit.

